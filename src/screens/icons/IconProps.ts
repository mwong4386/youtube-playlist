export interface IconProps {
  className?: string;
  size?: number | string;        // default 24
  strokeWidth?: number;          // default 2 for outline icons
  title?: string;                // optional accessible title
}
